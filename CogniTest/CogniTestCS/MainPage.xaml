<Page
    x:Class="CogniTestCS.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CogniTestCS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="FakeDetect" Name="BtnFakeDetect" Click="BtnFakeDetect_Click" Margin="10,10,10,10" />
            <Button Content="Speak" Name="BtnSpeak" Click="BtnSpeak_Click" Margin="10,10,10,10" />
            <MediaElement Grid.Row="0" Name="MediaPlayer" />
        </StackPanel>
        <TextBlock Grid.Row="1" Name="LblResult" />

    </Grid>
</Page>
